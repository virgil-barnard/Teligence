services:
  gpt:
    image: teligence-gpt:latest
    build:
      context: .
      dockerfile: Dockerfile
    gpus: all
    stdin_open: true
    tty: true
    environment:
      VISUALIZE: "1"
      LOG_EVERY: "50"
      EVAL_EVERY: "50"
      ATTN_VIZ_EVERY: "50"
    volumes:
      - ./:/app
    command: ["python", "scripts/launcher.py"]

  proof_math:
    image: teligence-gpt:latest
    build:
      context: .
      dockerfile: Dockerfile
    gpus: all
    stdin_open: true
    tty: true
    profiles: ["proof"]
    environment:
      APP_ENTRY: "proof_math"
    volumes:
      - ./:/app
    command: ["python", "scripts/launcher.py"]

  icarus_affine:
    image: teligence-gpt:latest
    build:
      context: .
      dockerfile: Dockerfile
    gpus: all
    stdin_open: true
    tty: true
    profiles: ["icarus"]
    environment:
      APP_ENTRY: "icarus_affine"
    volumes:
      - ./:/app
    command: ["python", "scripts/launcher.py"]

  tensorboard:
    image: teligence-gpt:latest
    depends_on:
      - gpt
    ports:
      - "6006:6006"
    volumes:
      - ./:/app
    command: ["tensorboard", "--logdir", "/app/runs", "--host", "0.0.0.0", "--port", "6006"]
