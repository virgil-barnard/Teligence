# Project identity
PROJECT_NAME=myproj

# Base image for this project
BASE_IMAGE=ubuntu:24.04

# Image name/tag
IMAGE_TAG=agent-myproj:latest

# Repo bootstrap (optional)
REPO_URL=
REPO_DIR=/workspace/repo
REPO_BRANCH=

# Extra docker compose run args are handled via projects/<proj>/compose.yaml (recommended).
