services:
  agent:
    # Project-specific overrides go here (ports, extra volumes, devices, privileges).
    # Example:
    # ports:
    #   - "8000:8000"
    # volumes:
    #   - type: bind
    #     source: /data/shared
    #     target: /data
    #     read_only: true
    #
    # If you need to relax hardening defaults:
    # cap_add:
    #   - SYS_PTRACE
    # security_opt:
    #   - apparmor:unconfined
