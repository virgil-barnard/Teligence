{
  "$schema": "https://opencode.ai/config.json",

  // Keep this config non-secret. API keys should come from env vars or opencode auth storage.

  "shell": {
    "path": "/bin/bash",
    "args": ["-lc"]
  },

  // You can add rules/instructions files here.
  "instructions": [
    "AGENTS.md",
    ".cursor/rules/*.md"
  ]
}
